{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // SDK 面向浏览器与 SSR 双端运行，需要 fetch/Response 等 Web 标准类型
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  },
  "include": ["src/**/*.ts"]
}
